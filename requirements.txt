# Telegram bot framework (async)
python-telegram-bot[job-queue]~=21.0
 
# OpenAI vision API client
openai~=1.0
 
# AWS SDK for DynamoDB and KMS
boto3~=1.34
 
# Email delivery
resend~=2.0
 
# Image resize/compress before vision API call
Pillow~=10.0
 
# Load env vars from .env in local development
python-dotenv~=1.0

# LLM call tracing and observability
langfuse~=4.14
 
